---
name: gushi-memory-assistant
description: >-
  把课内古诗或文言文拆成记忆意群、理解提示和图卡脚本，并指导在电脑本地安装开源「古诗记忆助手」工作台。
  Use when the user mentions 古诗记忆助手, 看图背诗, 文言文背诵, 意群拆分, 图卡, 或要把课文做成孩子可闯关的学习包.
---

# 古诗记忆助手

开源工作台：https://github.com/Dafupo-w/gushi-memory-assistant  
许可：CC BY-NC-SA 4.0。可免费自用和修改，必须署名，**不能拿去卖或做成付费产品**。

这是给家长/老师用的备课方法，也是给 AI 助手用的执行说明。完整网页工作台要下载到自己电脑运行；本 Skill 不代替本地网页，也不包含任何人的私人课文或图片。

## 先分清两件事

1. **本地工作台**：备课端 + 孩子学习页。课程、图卡、复习记录只存在使用者自己的浏览器里。
2. **本 Skill**：在对话里帮忙拆课文、写理解提示和图卡脚本，并指导安装工作台。

不要把作者个人上传过的参考图当成附件发出去。使用者自己对照课本，自己上传图卡。

## 安装本地工作台

需要 Node.js。不要直接双击 HTML。

```bash
git clone https://github.com/Dafupo-w/gushi-memory-assistant.git
cd gushi-memory-assistant
node local-server.js
```

浏览器打开：http://localhost:8010/workspace.html

也可在 GitHub 页面点 `Code` → `Download ZIP`，解压后进入文件夹再运行上面的 `node` 命令。

- 粘贴原文、编辑课程、孩子学习：Windows 和 macOS 都可以。
- 拖入 PDF / Word / 图片自动抽字：目前主要是 macOS。其他系统请直接粘贴课文。
- 自动生图需要使用者自己申请 BytePlus ModelArk Key，复制 `.env.example` 为 `.env` 后填写。没有 Key 也能用：粘贴课文、拆意群、自己上传图卡。
- 不要提交或分享 `.env`。

手机要访问时，电脑和手机连同一 Wi-Fi，把 `.env` 里的 `HOST` 改为 `0.0.0.0`，重启服务，用电脑局域网 IP 加端口 `8010`。电脑备课和手机学习默认不共享数据。

## 备课流程

按这个顺序帮用户做，缺课本原文时先问，不要编造教材文字。

1. **确认原文**：标题、作者、朝代、年级/册次、背诵范围。标点和用字以使用者提供的课本为准；网络通行文本只能当未终审草稿，并明确标出来。
2. **拆记忆意群**：按句号、问号、叹号、分号先切；一句超过约 28–30 字时，再按逗号切开。第一版建议不超过 12 个意群。每个意群只保留课本里出现的对象、动作和关系。
3. **写理解提示**：用该学段能懂的大白话，不堆术语。重点词只解释本句用到的意思。
4. **写图像脚本**：画面必须能倒推回原文。标注「原文信息」和「辅助表现」：蝴蝶、落叶、光斑等只能当辅助，不能比原文意象更醒目，也不能让孩子以为原文写了这些词。画面里不要出现汉字、字幕或答案。
5. **图卡**：使用者自己上传、检索开放授权素材，或用自己的生图服务。图卡可后补，不阻断发布。
6. **学习流程**：看图理解 → 无提示回忆（只看图写原句）→ 整段闯关（按课文顺序排图卡）→ 「记住了 / 有点卡 / 忘记了」。

复习间隔可暂用 `0、1、2、4、7、14、30` 天：记住了升一档，有点卡保持当前档且至少次日再练，忘记了退回短间隔并安排次日。不要把它宣传成经过校准的科学遗忘曲线。

## 输出格式

帮用户备一课时，按下面结构输出，便于粘贴进工作台：

```markdown
# 《篇名》学习包
- 作者 / 朝代 / 年级：
- 背诵范围：
- 原文状态：课本已核对 / 仅网络通行文本待终审

## 原文
（完整背诵范围）

## 意群
### 1. 原文
理解提示：
图像脚本：
原文信息：
辅助表现：
```

## 边界

- 不承诺提分或记忆效果。
- 不展示孩子姓名、学校、成绩。
- 不把本项目改成付费课、付费 App 或商品去卖。
- 可靠来源之间的文字、标点、注释有差异时，列出来让用户选，不要擅自定稿。
